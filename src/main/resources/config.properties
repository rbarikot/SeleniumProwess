# Base URL for the application
base.url=http://10.119.32.127:8080/da/

# Browser configuration
browser=chrome
headless=false

# Timeouts in seconds
implicit.wait=10
explicit.wait=15
page.load.timeout=30

# Screenshot settings
screenshot.path=test-output/screenshots
take.screenshot.on.failure=true

# Test data
test.data.path=src/test/resources/testdata

# Execution mode
execution.mode=grid
# Options: local, grid, remote

# Grid settings (if execution.mode=grid)
grid.url=http://selenium-hub:4444/wd/hub
#grid.url=http://localhost:4444

# Remote settings (if execution.mode=remote)
remote.url=http://localhost:4444
#remote.url=http://localhost:4444/ui/